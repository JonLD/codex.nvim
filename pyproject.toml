[project]
name = "codex-nvim-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastmcp",
  "pynvim",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
py-modules = []
